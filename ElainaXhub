-- ==========================================
-- ELAINA X HUB - NATIVE KEY SYSTEM (100% WORK)
-- ==========================================
local CorrectKey = "ElainaPeak"
local DiscordLink = "https://discord.gg/XQXyGyFPsw"
local MainScriptURL = "https://raw.githubusercontent.com/DarnyxSoTuff/Elain/refs/heads/main/Elaina-Keyless"

-- Dọn dẹp GUI cũ nếu đã tồn tại
if game:GetService("CoreGui"):FindFirstChild("ElainaKeyGui") then
    game:GetService("CoreGui").ElainaKeyGui:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ElainaKeyGui"
ScreenGui.ResetOnSpawn = false

-- Gắn UI vào Executor
local parentTarget = game:GetService("CoreGui")
pcall(function()
    if gethui then parentTarget = gethui() end
end)
ScreenGui.Parent = parentTarget

-- Khung Bảng Chính
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 320, 0, 210)
MainFrame.Position = UDim2.new(0.5, -160, 0.5, -105)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Color = Color3.fromRGB(130, 90, 255)
UIStroke.Thickness = 1.5
UIStroke.Parent = MainFrame

-- Tiêu Đề
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.Text = "Elaina X Hub - Key System"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.SourceSansBold
Title.Parent = MainFrame

-- Ô Nhập Key
local KeyInput = Instance.new("TextBox")
KeyInput.Size = UDim2.new(0.85, 0, 0, 36)
KeyInput.Position = UDim2.new(0.075, 0, 0.24, 0)
KeyInput.BackgroundColor3 = Color3.fromRGB(32, 32, 40)
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.PlaceholderText = "Nhập Key vào đây..."
KeyInput.PlaceholderColor3 = Color3.fromRGB(140, 140, 150)
KeyInput.Text = ""
KeyInput.TextSize = 14
KeyInput.Font = Enum.Font.SourceSans
KeyInput.ClearTextOnFocus = false
KeyInput.Parent = MainFrame

local InputCorner = Instance.new("UICorner")
InputCorner.CornerRadius = UDim.new(0, 6)
InputCorner.Parent = KeyInput

-- Nút Xác Minh Key
local VerifyBtn = Instance.new("TextButton")
VerifyBtn.Size = UDim2.new(0.85, 0, 0, 36)
VerifyBtn.Position = UDim2.new(0.075, 0, 0.47, 0)
VerifyBtn.BackgroundColor3 = Color3.fromRGB(120, 80, 255)
VerifyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
VerifyBtn.Text = "XÁC MINH KEY"
VerifyBtn.TextSize = 14
VerifyBtn.Font = Enum.Font.SourceSansBold
VerifyBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 6)
BtnCorner.Parent = VerifyBtn

-- Nút Get Key (Copy Discord)
local GetKeyBtn = Instance.new("TextButton")
GetKeyBtn.Size = UDim2.new(0.85, 0, 0, 36)
GetKeyBtn.Position = UDim2.new(0.075, 0, 0.70, 0)
GetKeyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
GetKeyBtn.TextColor3 = Color3.fromRGB(200, 200, 220)
GetKeyBtn.Text = "GET KEY (COPY LINK DISCORD)"
GetKeyBtn.TextSize = 12
GetKeyBtn.Font = Enum.Font.SourceSans
GetKeyBtn.Parent = MainFrame

local GetBtnCorner = Instance.new("UICorner")
GetBtnCorner.CornerRadius = UDim.new(0, 6)
GetBtnCorner.Parent = GetKeyBtn

-- Xử lý Logic
VerifyBtn.MouseButton1Click:Connect(function()
    local input = KeyInput.Text:gsub("%s+", "")
    if input == CorrectKey then
        VerifyBtn.Text = "KEY CHÍNH XÁC! ĐANG TẢI HUB..."
        VerifyBtn.BackgroundColor3 = Color3.fromRGB(40, 180, 90)
        task.wait(0.8)
        ScreenGui:Destroy()
        
        -- Load Script Chính Của Bạn
        loadstring(game:HttpGet("https://raw.githubusercontent.com/DarnyxSoTuff/Elain/refs/heads/main/Elaina-Keyless"))()

    else
        VerifyBtn.Text = "KEY SAI! VUI LÒNG THỬ LẠI"
        VerifyBtn.BackgroundColor3 = Color3.fromRGB(210, 50, 50)
        task.wait(1.5)
        VerifyBtn.Text = "XÁC MINH KEY"
        VerifyBtn.BackgroundColor3 = Color3.fromRGB(120, 80, 255)
    end
end)

GetKeyBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(DiscordLink)
    elseif toclipboard then
        toclipboard(DiscordLink)
    end
    GetKeyBtn.Text = "ĐÃ SAO CHÉP LINK DISCORD!"
    task.wait(1.5)
    GetKeyBtn.Text = "GET KEY (COPY LINK DISCORD)"
end)
